<?xml version="1.0" encoding="UTF-8"?>
<!--
  The synthetic adapter's mapping, named by the crate's mainEntity because every
  adapter names one. A stub: it produces nothing, and nothing runs it, because
  no Bridge exists and how a Bridge invokes a mapping is not yet specified.
-->
<xsl:stylesheet version="3.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
  <xsl:mode on-no-match="shallow-skip"/>
</xsl:stylesheet>
