# adapter/profile/must — Agent Context

- **A `.ttl` here is a check over every adapter's crate graph**, discovered and run
  by `rocrate-validator` rather than imported, and `../../../shapes/CLAUDE.md` holds
  for it. Shapes a Python check applies to some other file the crate names belong in
  `../../../shapes/`.
