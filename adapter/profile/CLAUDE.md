The `.ttl` files in `must/` are shapes: `../../shapes/CLAUDE.md` holds for them.

A requirement lands with its tests in `../../tests/adapter_profile/`, in the same
commit: `python3 -m pip install --group dev`, then `python3 -m pytest`.
